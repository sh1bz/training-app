# training-app

A minimal SvelteKit + Supabase one-pager.

## Setup

```bash
npm install
cp .env.example .env
# fill in PUBLIC_SUPABASE_URL and PUBLIC_SUPABASE_ANON_KEY
npm run dev
```

Open http://localhost:5173 — the page says "Hello World" and shows the Supabase client connection status.
